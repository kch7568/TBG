<!-- rounded_button_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- 버튼 배경색 설정 -->
    <corners android:radius="12dp" /> <!-- 둥근 모서리 반경 설정 -->
    <stroke android:width="1dp" android:color="@android:color/black" /> <!-- 테두리 설정 (선택사항) -->
</shape>
